{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python dashboard.py",
    "healthcheckPath": "/api/files",
    "restartPolicyType": "ON_FAILURE"
  }
}
